<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_bg"
    android:orientation="vertical">

    <include
        layout="@layout/toolbar_view" />

    <net.kamfat.omengo.component.tablayout.TabLayout xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/tab_layout"
        style="@style/tablayout_style"
        app:tabIndicatorColor="@color/colorPrimary"
        app:tabMode="scrollable"
        app:tabSelectedTextColor="@color/colorPrimary"
        app:tabTextAppearance="@style/my_tab_apperance" />

    <include
        layout="@layout/item_refresh_list_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>